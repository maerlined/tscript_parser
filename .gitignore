# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Parsed output (generated files, not source) — keep sample_parsed.csv as an example
run-with-python/parsed/*.csv
!run-with-python/parsed/sample_parsed.csv

# macOS
.DS_Store

# Claude Code (local agent config, not project source)
CLAUDE.md
.claude/

